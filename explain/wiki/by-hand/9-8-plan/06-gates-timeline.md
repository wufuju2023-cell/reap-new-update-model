# 06 — 门、时间线、资源与回退

## 依赖资源（当前状态）
| 资源 | 状态 | 行动 |
|---|---|---|
| GPU 实例 | **已关闭** | 重开（MI300X 或等价；显存 ≥80GB 建议） |
| 模型权重 | NFS 持久（REAL 15G / Qwen 19G in `/mnt/workspace/models`） | 实例重开后校验 sha |
| v1-1 服务栈 | 代码已入库（`v1-1-agentic-tool`） | 重开实例后按 next-1/01 部署 |
| 2k 题生成器 | R1 有（受限变体）/ R2 无 | R2 需开发（本计划工程量①） |
| 跨题 RL | 无 | 需开发（工程量②） |

## 时间线（以"实例重开"为 T0，纯执行时间估算）
| 阶段 | 内容 | 预算 | 门 |
|---|---|---|---|
| T0+0.5h | 实例恢复：部署 server + smoke 3/3 | 0.5h | value 1.786/1.962/3.216 |
| T0+2h | 工具调用探测（两模型 8 题×2） | 1.5h | 评分表产出；REAL/Qwen 各得 0-100 |
| 分支 A（REAL ≥70） | 直接 04→05 | — | — |
| 分支 B（Qwen 路线） | Qwen value head 训练（03） | 0.5-1 天 | artifact 校验门 |
| 分支 C（都 <70） | SFT 工具调用（方案1） | 2-3 天 | 探测重跑 ≥70 |
| 语料 | R1 数百题（立即）+ R2 2k（开发） | R1 0.5 天 / R2 2-4 天 | 验证门通过率/类平衡 |
| 课程+TTRL | 100 题试跑 → 扩 2k | 1-3 天 | 成功树产出率 |
| RL | 最小轮次 → 扩规模 | 3-7 天 | 奖励提升 + 无遗忘（KL 监控） |

## 回退策略（每阶段都有"体面退出"）
- 探测失败但 SFT 也无效 → 回到"纯 MCTS+TTT 无工具"（V1 模式），工具环仅做证据（B 通道）；
- value head（Qwen）训练失败 → 用 REAL full-v3 + Qwen policy 混合（需 runtime 支持，或继续 REAL 全栈）；
- RL 不稳定 → 降级为"多轮 TTT + 课程"（现有能力），RL 延后；
- 2k 语料质量不足 → 缩小到 300-500 高质题，优先把链跑通（先诊断后扩张）。

## 立即行动（实例重开前可做的）
1. **写工具调用探测脚本**（`tool-call-probe/`，本地开发，实例一开就跑）；
2. **R1 语料种子**：选 50-100 个抽象代数 statement（群/环/域）准备为种子池；
3. **R2 生成器设计冻结**（9-7 系列 06 的流水线，写成可执行脚本骨架）；
4. 本文档 + 9-7 系列为唯一执行依据（忽略未实现 spec）。
