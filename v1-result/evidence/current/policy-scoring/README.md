# 固定候选评分：数值一致，未获得实用提速

输入是一条固定提示，共103个词元，以及4个固定候选，共47个待评分词元。默认逐词元评分与第二版延后读取结果的实现，各预热1次、测量3次。新实现提前把每个候选的词元编号放到GPU上，保持模型每次前向计算的形状与顺序；输出分数与默认实现精确一致。

默认实现中位耗时1.65942秒，新实现为1.62876秒，比值1.0188。因此仍保留默认逐词元实现。这组固定评分测试没有建立稳定、实用的速度收益，也不代表生成或整个题内训练流程加速。

[原始报告](files/report.json)保留逐项数值、配置和计时；[原始材料](raw.zip)与[成员清单](manifest.json)绑定报告哈希。`files/`是阅读副本，压缩包保存清单内完整材料。此前候选分块评分（`candidate_chunks`）出现数值不一致，第一版延后读取结果的测试也没有带来实用提速；两者保留在项目历史实验中，本阶段包内原始材料仅包含第二版报告。大模型与张量不在包内。
