# 便携双服务Lean复现：本地验收

本阶段交付`portable_lean.py`及小型容器模板，将已有的题目调度、双服务分配、证明搜索和独立验证串成复现入口。这里检查的是包装与恢复协议，没有新增GPU、Lean或Podman运行实验。

| 模式 | 准备与验收范围 |
|---|---|
| 默认原镜像 | 显式使用原d207 CPU镜像，固定历史声明和输入；每次运行生成新的题族、任务和会话标识。模板来自已完成的真实实验，本次只验收包装修改。 |
| 显式`--rebind-cpu-image` | 自建镜像建立新的环境与源码身份，旧收据只留作来源说明。准备阶段不生成编译成功收据、最终任务哈希或会话标识；必须先完成两次断网预检，才确定执行计划并调度GPU。本次测试模拟了预检协议，新镜像兼容性仍需实际运行确认。 |

[最终回执](files/rebind-final-receipt.json)：Windows14项5.029秒、WSL14项10.481秒全部通过；列明源码前后SHA一致。测试覆盖新旧路径、独占输出、来源篡改、未知启动不重发、容器网络/镜像参数、不同run生成不同SID、旧/失败/缺失预检收据拒绝，以及两份实际server CLI参数解析。重绑定测试使用明确的合成预检收据，未冒充新Lean检查。

原9项阶段和首次family长度拒绝日志均保留。完整日志、前后源码SHA及测试命令在[raw.zip](raw.zip)，[manifest](manifest.json)绑定全部原始材料；files仅提供JSON阅读副本。原代码、历史证据和冻结归档不改写。

[复现命令与两个GPU服务启动参数](../../../source/current/README.md)说明环境、运行顺序和保存位置。实际进程退出码为0与总报告成功必须同时成立；失败或结果未知时导出的文件只作现场证据，不能当作实验通过。此阶段没有大张量。
