# 同卡双服务与真实 Lean 调度

两个独立进程在同一张GPU上分别加载7B模型，每个服务最多保留一个会话。两次证明尝试都使用此前混合训练发布的同一版参数，尝试期间不训练、不更换版本。系统在搜索前保存服务分配记录，确认会话释放后才让下一题占用该服务；本阶段没有启用最新版本自动选择。

| 尝试编号末尾 | 任务与独立结果 | 证明步骤数 | 整体证明检查 / 逐步复验秒 |
|---|---|---:|---:|
| 00001 | 证明完整否定`¬ (∀ n : Nat, n = 0)`，从而反驳原命题 | 2 | 6.848 / 6.369 |
| 00002 | 证明`∀ n : Nat, n = n` | 1 | 6.988 / 6.210 |

原始定义在搜索前显式展开，但不提供证明。两个证明分别通过独立的整体检查、公理依赖检查和逐步复验，安装后的17文件数据包与验证输出逐字节一致。两次搜索、两次预检、两次独立验证容器均退出0；验证容器断网，使用固定CPU镜像和Linux命名卷。

批次共75.318809秒，最多同时执行两个搜索任务、一个验证任务，待验证阶段最多占用两个位置，没有未知结果。按搜索日志首事件至末事件计，两个区间重叠 **7.042941122秒**；按首次选择至最终确认计，重叠 **7.015999063秒**。搜索容器生命周期重叠约31.1485秒。这些区间混合了CPU、HTTP和模型调用，不能当作GPU内核同时执行的测量。本批搜索与独立验证容器没有实际重叠，不能据此声称验证流水线带来收益；也不能把另一固定请求测试的1.93倍套用为本次全流程加速。

## 证据入口

实际服务进程为2692和2749，端口为8761和8762；固定参数版本以`01031555…65c98e`标识，各题内版本为0。这些技术标识用于关联下列原始记录。

- [批次与调度报告](files/collected/host/batch-report.json)、[campaign报告](files/collected/host/campaign-report.json)。
- [本地独审](files/collected/independent-review.json)：重新核对77份冻结成员、287份本地导出文件、28份远端导出文件、56份实际部署源码hash、原始路由与退役链。
- [原始HTTP集合](files/collected-remote/wire-combined.json)：10个作业均完成且HTTP状态为200或201，没有训练请求；提示、响应标识、生成步骤、原始对数概率和负距离价值逐一对应搜索事件。
- [服务退出](files/collected-remote/files/suite-exit.json)：两个服务进程及协调器均退出0；会话释放完成后才写结束信号。
- [完整材料](raw.zip)、[成员清单](manifest.json)。大型GPU张量未下载或打包；本地核对终态逻辑和快照清单哈希，未重新解码大张量。

旧在线TTT通信审计限定了搜索备份价值的训练配置，不能直接套用到本阶段的混合模型采集器。因此独审保留原始字节解码，另核固定版本采集的请求与响应，没有放宽旧审计条件。两题是预先选定的分支覆盖，不是成功率、长期稳定性或吞吐基准测试。实际运行的固定代码与后来的最新版本选择实现分开保存。
