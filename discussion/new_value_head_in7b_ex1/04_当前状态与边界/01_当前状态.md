# 当前状态

更新时间：2026-08-29。Radeon 的首个 REAL7B full-v3 课程案例已经数学成功并通过独立 Lean，当前只做无重发的发布封存；ModelScope 新调度、S18、20题矩阵、random 对照和非必要审计暂停。

## 临时选择

REAL7B full-v3 是当前最好的可执行 value head，但尚未通过完整 20 题稳定性门，不能称最终默认。S18 新 distance head 保留正式候选；其 P2 与随机臂的完整轨迹逐项相同，当前没有端到端收益证据。

## 展示案例

- 已完成：[F1 匹配随机头因果案例](../02_成功案例/01_F1匹配随机头因果案例.md)。F1 有适当难度，不是首候选直接闭合；full-v3 经过真实 MCTS、3 次被后续 generation 消费的 LoRA+categorical-head 联合 TTT，得到完整证明并通过独立 Lean，而精确随机臂同预算失败。
- 已完成：[H1 平方望远镜课程成功](../02_成功案例/02_H1平方望远镜课程成功/README.md)。Radeon 的 `CourseSquareTelescope` 在 checkpoint 20 自然证明成功，step 7 与 step 15 两次 LoRA+categorical-head 联合 TTT 均被后续 generation/value 前向消费，完整学生 proof 已通过固定禁网 Lean 验收；终端 success-learn 也已真实应用至 v3。无重发恢复后已 seal、发布 `exp-078cea5460cb-01` 并 retire，完整闭环完成。
- 该案例证明 full-v3 的课程学习、MCTS、联合 TTT 与 Lean 验收链可以真实跑通；它不是随机头 matched pair，因此相对因果优势仍以 F1 和正式矩阵为准。原立方 H1 fresh session `course-f86f5c4fcb50-01` 已精确继承本课 experience；第一完整更新窗到 checkpoint 49，step 48 联合 learn v0→v1，LoRA/value head/optimizer 均真实变化，且 v1 后续 generation/value 请求已开始，当前继续自然运行。

原 H1 只有在得到完整证明且独立 Lean 验收通过后才作为更高难度案例加入 `02_成功案例/`。若失败，其证据只进入正式评估报告。

## 本阶段交付门

- 至少完成一条训练工件 -> 运行合同 -> MCTS -> 题内 TTT -> 完整 proof -> 独立 Lean -> snapshot/retire 的真实证据链。
- 对应 matched-random 同预算结果齐全。
- 报告明确区分 value 帮助、误导和不可辨认作用。
- 20 题未完成前结论保持“临时首选”；最终同时回答“谁最好”与“是否已经够好”。
